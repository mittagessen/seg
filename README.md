trainable 2-stage segmenter
---------------------------

1. ReNet-style pixel labelling returning baseline(-ish) instances
2. feed each baseline instance into a mask refinement network with the original image.
